# Piper voices

Voice model files are downloaded here by:

```bash
npm run piper:install
```

The application only accepts voice IDs listed in `voices/manifest.json`. Add more Piper voices to that manifest and rerun the installer when you want to support additional languages or speakers.
